Имеется таблица Product с двумя столбцами: Product_ID и Price. Как будут различаться результаты двух приведенных ниже запросов?
SELECT MAX(Price) FROM Products 
SELECT ROUND(MAX(Price)) FROM Product GROUP BY Price;  
В первом случае запрос выдаст максимальную цену такой, какая она есть на самом деле.
Во втором случае запрос выдаст максимальную округленную целочисленную часть.
